define(['setCss'], function( setCss ) {
  setCss('');
  // If we can figure out a way to clean up
  // modElem and inputElem, we should
  // We _could_ put them on the Modernizr object
  // for reuse or deletion.
});
